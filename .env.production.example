# PRODUCTION ENVIRONMENT VARIABLES - REQUIRED FOR SECURITY
# Copy this file to .env.production and fill in secure values

# NextAuth Configuration (REQUIRED)
NEXTAUTH_URL=https://yourdomain.com
NEXTAUTH_SECRET=super-secure-random-string-at-least-32-characters-long

# Admin Authentication (REQUIRED)
ADMIN_EMAIL=admin@evangelosommer.com
ADMIN_PASSWORD=create-a-very-strong-password-here

# JWT Security (REQUIRED)
NEXTAUTH_JWT_SECRET=another-super-secure-random-string-different-from-above

# Database (if using)
DATABASE_URL=your-production-database-url-here

# Security Settings
STRICT_IP_VALIDATION=true
NODE_ENV=production

# ⚠️  CRITICAL SECURITY WARNING:
# DO NOT deploy to production without setting ALL required variables above.
# The system will fail authentication if ADMIN_PASSWORD is not set in production.
# Use strong, unique passwords and secrets.