╔══════════════════════════════════════════════════════════════════════════════╗
║                    SPF RECORD UPDATE - BLUEHOST INSTRUCTIONS                 ║
╚══════════════════════════════════════════════════════════════════════════════╝

✅ STEP 1: LOG INTO BLUEHOST CPANEL
   → https://my.bluehost.com/
   → Click "Advanced" or "cPanel" button
   → Find your account for evangelosommer.com

✅ STEP 2: NAVIGATE TO EMAIL DELIVERABILITY
   → In cPanel, scroll to "Email" section
   → Click "Email Deliverability"
   → Wait for the tool to load (may take 10-20 seconds)

✅ STEP 3: SELECT YOUR DOMAIN
   → Look for: evangelosommer.com
   → Click "Manage" next to the domain name

✅ STEP 4: UPDATE SPF RECORD
   → Find the "SPF" section
   → You should see Bluehost's suggested record:

     v=spf1 +ip4:162.241.217.72 +include:sendgrid.net ~all

   → Click "Install the suggested record" or "Update"
   → Alternatively, click "Customize" and paste:

     v=spf1 +ip4:162.241.217.72 +include:sendgrid.net ~all

✅ STEP 5: SAVE CHANGES
   → Click "Save" or "Update Record"
   → Wait for confirmation message
   → Bluehost may show a green checkmark

✅ STEP 6: VERIFY (wait 30-60 minutes for DNS propagation)
   → Open Terminal on your Mac
   → Run this command:

     dig TXT evangelosommer.com +short | grep spf

   → You should see:

     "v=spf1 +ip4:162.241.217.72 +include:sendgrid.net ~all"

════════════════════════════════════════════════════════════════════════════════

📋 WHAT THIS SPF RECORD DOES:

+ip4:162.241.217.72
  → Authorizes Bluehost mail server IP
  → Required for Bluehost UI to manage the record
  → This server is blacklisted BUT not used for sending
  → Safe to include (SendGrid handles all actual sending)

+include:sendgrid.net
  → Authorizes SendGrid to send from your domain
  → THIS IS THE ACTIVE SENDER (all emails go through SendGrid)
  → Includes SendGrid's IP ranges automatically

~all
  → "Soft fail" for emails from unauthorized sources
  → Recommended by email standards (vs -all "hard fail")

════════════════════════════════════════════════════════════════════════════════

⚠️  IMPORTANT NOTES:

1. The Bluehost IP (162.241.217.72) appears blacklisted on some lists
   → This DOES NOT affect your email delivery
   → You're not sending from this IP (SendGrid handles sending)
   → It's only included so Bluehost UI can manage the record

2. This is a practical compromise:
   → Ideal SPF: v=spf1 include:sendgrid.net ~all (minimal)
   → Bluehost requires: The IP address to be included
   → Solution: Use Bluehost's format for easier management

3. SendGrid is your ONLY active sender:
   → All receipts, notifications, DMARC reports via SendGrid
   → mail.evangelosommer.com is NOT used
   → SPF validates correctly for SendGrid emails

════════════════════════════════════════════════════════════════════════════════

🔍 TROUBLESHOOTING:

ISSUE: Bluehost won't let me save the record
  → Make sure you included the IP: +ip4:162.241.217.72
  → Check for typos in the record
  → Try using "Install suggested record" button instead of manual entry

ISSUE: MXToolbox still shows SPF errors after 1 hour
  → DNS can take up to 24 hours to fully propagate
  → Run: dig TXT evangelosommer.com +short
  → If you see the new record in dig output, it's working

ISSUE: Worried about the blacklisted IP
  → Don't worry! You're not sending from it
  → SendGrid handles all email sending
  → The IP is only for Bluehost UI compatibility

════════════════════════════════════════════════════════════════════════════════

✅ EXPECTED MXTOOLBOX RESULTS AFTER UPDATE:

WILL BE FIXED:
  ✅ SPF lookup count (reduced from 11 to ~4-5)
  ✅ SPF validation passes
  ✅ Email authentication working

WILL REMAIN (SAFE TO IGNORE):
  ⚠️  308 Permanent Redirect (HTTP→HTTPS, correct behavior)
  ⚠️  mail.evangelosommer.com blacklisted (not used for sending)
  ⚠️  DMARC policy "none" (monitoring mode, will upgrade later)

════════════════════════════════════════════════════════════════════════════════

📖 For detailed explanation, see:
   - DNS_CONFIGURATION.md (comprehensive guide)
   - DMARC_UPGRADE_GUIDE.md (email authentication timeline)
   - test-sendgrid.js (test email sending)

🎯 Quick Test After Update:
   cd /Users/epsommer/projects/evangelo-sommer
   node test-sendgrid.js

════════════════════════════════════════════════════════════════════════════════
